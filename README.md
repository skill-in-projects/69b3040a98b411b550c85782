# SafePath - Frontend

## GitHub Pages

**URL:** https://skill-in-projects.github.io/69b3040a98b411b550c85782/

## Stack

React + Vite. Run `npm install` then `npm run dev` or `npm start`. Use `npm run lint` and `npm run format` for ESLint and Prettier.
